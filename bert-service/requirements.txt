# Keep this file synchronized with pyproject.toml for pip-based environments.
fastapi[standard]>=0.136.1,<0.137.0
transformers==5.14.1
torch==2.13.0
numpy>=2.0
datasets==5.0.1
accelerate==1.14.0
matplotlib==3.11.1

# Dev / dataset preparation extras:
#   python -m pip install -e ".[dev]"
#   python -m pip install -r requirements-dev.txt
