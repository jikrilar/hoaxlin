; Transport headroom above the 24 MiB application video limit lets Laravel validate uploads.
; POST includes multipart overhead, so it must exceed the per-file transport limit.
upload_max_filesize=32M
post_max_size=34M
